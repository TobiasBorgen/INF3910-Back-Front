export const CONFIG = {

	/* Socket.io configuration.
	 * Remote server address and port.
	 */
	SOCKET_REMOTE: '127.0.0.1',
	SOCKET_PORT: 3000,

	/* Cloud Connect configuration.
	 */
	THING_TYPE: '121'
}
